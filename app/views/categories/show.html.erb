<div class="text-center "><h1>Category:<%=@category.name%></h1>

<h2>Articles List</h2>
<% if logged_in? && current_user.admin %>
<%= link_to "Edit Category profile", edit_category_path(@category), class:"btn btn-outline-info m-2"%>
<%end%>
</div>
<div class="flickr_pagination">
    <%= will_paginate @articles, :container => false %>
  </div>
<%= render 'articles/article'%>
<div class="flickr_pagination">
    <%= will_paginate @articles, :container => false %>
  </div>