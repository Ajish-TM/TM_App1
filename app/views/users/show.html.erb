<div class="text-center"><h1><%=@user.username%>'s Profile</h1>

<%= gravatar_for @user, size:200%>
<h2>Articles List</h2>
<% if logged_in? && @user==current_user%>
<%= link_to "Edit user profile", edit_user_path(@user), class:"btn btn-outline-info m-2"%>
<%end%>
</div>
<div class="flickr_pagination">
    <%= will_paginate @articles, :container => false %>
  </div>
<%= render 'articles/article'%>
<div class="flickr_pagination">
    <%= will_paginate @articles, :container => false %>
  </div>